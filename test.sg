Inner :: struct
{
	a: i32;
}

Inner :: test2 :: (self): i32 
{
	self.a
}

Values :: struct
{
    a: i32;
	b: i32;
	c: i32;
	d: Inner;
}

Values :: sum :: (self): i32 
{ 
	test :: (): i32 {
		self.a + self.b + self.c + self.d.a
	};

	test()
}

Values :: mul :: (self): i32
{
	self.a * self.b * self.c
}

main :: (): i32
{
	s := Values { 
		a: 11,
		b: 22, 
		c: 33,
		d: Inner {
		    a: 420,
		},
	};

	s.d.a = 0;

	result := s.d.test2();

	printf("%d", s.mul());

	0
}

