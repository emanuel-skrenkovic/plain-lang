test :: (initial: number, end: number): number {
    acc := initial;
    while acc < end {
        acc = acc + 1;
    };

    if acc == 42069 {
        acc + 1
    } else {
        acc * 2
    }
}

c :: test(0, 42069);
