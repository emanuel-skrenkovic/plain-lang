add2 :: (a: number, b: number)
{
    c := 0;

    for i := 0; i < 15; i = i + 1 {
        c = c + i;
    }

    a + b + c
}

main :: ()
{
    result :: add2(3, 1);
    f_str :: "%d\0";
    printf(f_str, result);
    0
}
